[env:esp32dev]
platform = espressif32
board = esp32dev
framework = espidf
monitor_speed = 115200

; Minimal and explicit build flags for predictable codegen.
build_flags =
    -std=gnu11
    -Wall
    -Wextra
    -Werror
